# Tarang — FastWhisper Sidecar dependencies
# Install with:  pip install -r requirements.txt

# Core transcription engine (runs OpenAI Whisper models locally via CTranslate2)
faster-whisper>=1.0.0

# No Flask needed — uses Python's built-in http.server (zero extra deps)
